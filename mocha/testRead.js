const assert = require('assert');
const Article = require('../api/database/Article');

let article;

// beforeEach((done) => {
//     article = new Article({  title: 'test' });
//     article.save()
//         .then( () => done());
// });

describe('READ // Lire les information', () => {
    // it('READ 1 // Recherche d article avec le title Test', (done) => {
    //     Article.findOne({ name: 'test' })
    //         .then((art) => {
    //             assert(article.title === 'test'); 
    //             done();
    //         });
    // })
})